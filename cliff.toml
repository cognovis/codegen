# git-cliff configuration — changelog from conventional commits
# https://git-cliff.org/docs/configuration

[changelog]
header = """
# Changelog

All notable changes to this project are documented here.
Generated by [git-cliff](https://git-cliff.org) from conventional commits.
"""
# template for the changelog body
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### {{ group | striptags | trim | upper_first }}
    {% for commit in commits %}
        - {% if commit.scope %}**{{ commit.scope }}:** {% endif %}\
            {{ commit.message | upper_first | trim }}\
            {% if commit.breaking %} [**breaking**]{% endif %} \
            ({{ commit.id | truncate(length=7, end="") }})
    {% endfor %}
{% endfor %}
"""
# template for the changelog footer
footer = """
<!-- generated by git-cliff -->
"""
# remove the leading and trailing whitespace
trim = true

[git]
# parse the commits based on https://www.conventionalcommits.org
conventional_commits = true
# filter out the commits that are not conventional
filter_unconventional = true
# process each line of a commit as an individual commit
split_commits = false
# regex for preprocessing the commit messages
commit_preprocessors = [
  # remove Cognovis bead references from the scope, e.g. fix(fmgt-4bg): -> fix:
  { pattern = '\((fmgt|clc|cg)-[a-z0-9]+\)', replace = "" },
]
# regex for parsing and grouping commits
commit_parsers = [
  { message = '^feat', group = "<!-- 0 -->Features" },
  { message = '^fix', group = "<!-- 1 -->Bug Fixes" },
  { message = '^perf', group = "<!-- 2 -->Performance" },
  { message = '^refactor', group = "<!-- 3 -->Refactoring" },
  { message = '^docs', group = "<!-- 4 -->Documentation" },
  { message = '^(style|test|build|ci)', group = "<!-- 5 -->Miscellaneous" },
  { message = '^chore\(release\): prepare for', skip = true },
  { message = '^chore: bump version', skip = true },
  { message = '^chore', skip = true },
  { message = '^merge', skip = true },
  { body = '.*security', group = "<!-- 6 -->Security" },
]
# protect breaking changes from being skipped
protect_breaking_commits = true
# filter out the commits that are not matched by commit parsers
filter_commits = false
# sort the tags topologically
topo_order = false
# sort the commits inside sections by newest first
sort_commits = "newest"
